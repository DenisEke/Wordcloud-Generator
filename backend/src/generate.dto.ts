export default class GenerateDTO {
    width: number;
    height: number;
    maxWords: number;
    scale: number;
    minFontSize: number;
    maxFontSize: number;
    fontStep: number;
    repeat: boolean;
    mode: string;
    colors: Array<string>;
    words: Array<Object>
}